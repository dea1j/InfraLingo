# InfraLingo
InfraLingo uses Lingo.dev to perfectly translate complex cloud security audits and architecture documentation into a developer's native language, without breaking the underlying Terraform syntax.
